---
import { buttonVariants } from "../ui/button"
import { Icon } from "astro-icon/components"
---

<section class="w-full py-16 md:py-24 bg-card/30">
  <div class="container max-w-4xl mx-auto px-4 md:px-6 text-center">
    <div class="space-y-6">
      <div class="inline-block rounded-lg bg-muted px-3 py-1 text-sm">Get Started</div>
      <h2 class="text-3xl md:text-5xl font-bold">Ready to Build with UCAN?</h2>
      <p class="text-muted-foreground text-lg max-w-2xl mx-auto">
        Start implementing decentralized authorization in your applications today. Choose from multiple programming languages and frameworks.
      </p>
      
      <div class="flex flex-col sm:flex-row gap-4 justify-center items-center mt-8">
        <a href="/specification/" class={buttonVariants({ variant: "default", size: "lg" })}>
          <Icon class="size-4 mr-2" name="lucide:book-open" />
          Read Full Spec
        </a>
        <a href="/getting-started/" class={buttonVariants({ variant: "secondary", size: "lg" })}>
          <Icon class="size-4 mr-2" name="lucide:zap" />
          Get Started
        </a>
        <a href="/libraries/" class={buttonVariants({ variant: "outline", size: "lg" })}>
          <Icon class="size-4 mr-2" name="lucide:code-xml" />
          Browse Libraries
        </a>
      </div>

            <div class="mt-12 p-6 bg-background/50 rounded-lg border border-border/50">
        <h3 class="text-lg font-semibold mb-4">Available Libraries</h3>
        <div class="flex flex-wrap justify-center gap-4 text-sm text-muted-foreground">
          <span class="flex items-center gap-2">
            <Icon class="w-4 h-4" name="lucide:code-xml" />
            JavaScript
          </span>
          <span class="flex items-center gap-2">
            <Icon class="w-4 h-4" name="lucide:code-xml" />
            Rust
          </span>
          <span class="flex items-center gap-2">
            <Icon class="w-4 h-4" name="lucide:code-xml" />
            Go
          </span>
        </div>
      </div>
    </div>
  </div>
</section>
